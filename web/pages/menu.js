import { map } from 'ramda'

import styled from 'styled-components'

import Index from './index'

const Blur = styled.div`
	filter: blur(5px);
`
const Menu = ({ className }) => <div />
export default Menu
